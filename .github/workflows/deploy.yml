name: Deploy to Host Papa

# Runs on every push to main, and can also be triggered by hand
# from the repo's Actions tab.
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Host Papa via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:   ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps          # encrypted FTP over TLS (port 21)
          port:     21

          # Where the files go ON THE SERVER.
          # The FTP account (dshackley@shackleyhomes.co.uk) logs in at
          # /home/shack346, so we point at the site's subfolder here.
          # Result: /home/shack346/jjentertainments.co.uk/
          server-dir: jjentertainments.co.uk/

          # Things that should NOT be uploaded to the live site:
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/screens/**
            **/README.md
            **/config.example.php
            **/.ftp-deploy-sync-state.json
