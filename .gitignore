# Local screenshots — not part of the website, kept off the public repo
screens/

# Server-only settings — keeps your real email out of the public repo
config.local.php

# State file created by the FTP deploy action
.ftp-deploy-sync-state.json

# OS / editor cruft
.DS_Store
Thumbs.db
